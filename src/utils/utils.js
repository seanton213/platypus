export const inflate = (list, key) => {
    list.array.forEach(element => {
        var result = arr.reduce(function(map, key) {
            map[obj.key] = obj.val;
            return map;
        }, {});        
    });
}